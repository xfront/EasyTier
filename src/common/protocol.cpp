#include <easytier/common/protocol.hpp>

// Protocol implementation
// Most protocol logic is inline in the header (serialization/deserialization).
// This file is reserved for future extensions (e.g., compression, additional codecs).

namespace easytier {

// Placeholder for any non-inline protocol functions that may be needed later.

} // namespace easytier
